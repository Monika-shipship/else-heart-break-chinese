
# Reserved for heavy calculations.
# 用于繁重计算。

number SUM(array a)
    var sum = 0
    loop x in a
        sum += x
    end
    var hugin = Connect("Hugin")
    hugin.Print("Sum calculation result: / 总和计算结果：" + sum)
    return sum
end

number PRODUCT(array a)
    var product = 0
    loop x in a
        product *= x
    end
    var hugin = Connect("Hugin")
    hugin.Print("Product calculation result: / 乘积计算结果：" + product)
    return product
end