ClearText()
Print("   _____          _         _____")
Print("  / ____|        | |       / ____| ")
Print(" | |     ___   __| | ___  | |     __ _ _ __   ___ _ __ ")
Print(" | |    / _ \ / _` |/ _ \ | |    / _` | '_ \ / _ \ '__|")
Print(" | |___| (_) | (_| |  __/ | |___| (_| | |_) |  __/ |  ")
Print("  \_____\___/ \__,_|\___|  \_____\__,_| .__/ \___|_|")
Print("                                      | |            ")
Print("                                      |_|        ")
Print("")
Print("                     By ZDog Technology")
Print("                     作者 ZDog Technology")
Sleep(4)
ClearText()

levelone()
leveltwo()
levelthree()
levelfour()
levelfive()
winscreen()

void winscreen()
    PlaySound("Arcade music")
    loop
        ClearText()
        Print("You are now the proud owner of [Monetary Value Here]")
        Print("")
        Print("恭喜！")
        Sleep("0.5")
        ClearText()
        Print("You are now the proud owner of [Monetary Value Here]")
        Sleep("0.5")
    end
end

void levelone()
    loop
        ClearText()
        Print("欢迎使用 BankMaster 安全系统！")
        string input
        input = Input("请输入密码:")
        if input == "Guest"
            Print("访问已授予！")
            Sleep(1)
            break
        else
            Print("访问被拒绝！")
            Sleep(1)
        end
    end
end

void leveltwo()
    loop
        ClearText()
        Print("二级安全")
        string input
        array password = ["a","d","m","i","n"]
        input = Input("请输入密码:")
        if input == password[0] + password[1] + password[2] + password[3] + password[4]
            Print("访问已授予！")
            Sleep(1)
            break
        else
            Print("访问被拒绝！")
            Sleep(1)
        end
    end
end

void convert(array a)
 number count = Count(a) - 1
    loop x from 0 to count
        a[x] = IntToChar(CharToInt(a[x])+1)
        x += 1
    end
 return a
end

void levelthree()
    loop
        ClearText()
        Print("三级安全")
        array answer = "qbttxpse"
        array input = Input("请输入密码:")
        convert(input)
        number count = Count(input) - 1
        bool equal
        loop x from 0 to count
            if input[x] == answer[x]
                x+=1
                equal = true
            else
                equal = false
            end
        end
        if equal == true
            Print("访问已授予！")
            Sleep(1)
            break
        else
            Print("访问被拒绝！")
            Sleep(1)
        end
    end
end

void levelfour()
    loop
        ClearText()
        Print("四级安全")
        string input
        input = Input("请输入密码:")
        if input == "42"
            Print("访问已授予！")
            Sleep(1)
            break
        else
            Print("访问被拒绝！")
            Sleep(1)
        end
    end
end

void convert2(array a)
    number count = Count(a) - 1
    array new
    loop x from 0 to count
        new[x] = a[x]
    end
    loop x from 0 to count
        number cell = count - x
        a[x] = new[cell]
        x += 1
    end
  return a
end

void levelfive()
    loop
        ClearText()
        Print("五级安全")
        array answer = "sdrawkcab"
        array input = Input("请输入密码:")
        convert2(input)
        number count = Count(input) - 1
        bool equal
        loop x from 0 to count
            if input[x] == answer[x]
                x+=1
                equal = true
            else
                equal = false
            end
        end
        if equal == true
            Print("访问已授予！")
            Sleep(1)
            break
        else
            Print("访问被拒绝！")
            Sleep(1)
        end
    end
end